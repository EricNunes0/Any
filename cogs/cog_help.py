import discord
from discord.ext import commands
import datetime
import json

intents = discord.Intents.default()
intents.members = True
now = datetime.datetime.now()
now = now.strftime("%d/%m/%Y - %H:%M:%S")

#def get_prefix(bot, message):
#    with open('prefixes.json', 'r') as f:
#        prefixes = json.load(f)
#    return prefixes[str(message.guild.id)]
command_prefix = "a!"
bot = commands.Bot(command_prefix = "a!", intents=intents,  case_insensitive = True)

def cooldown(rate, per_sec=0, per_min=0, per_hour=0, type=commands.BucketType.default):
    return commands.cooldown(rate, per_sec + 60 * per_min + 3600 * per_hour, type)

command_prefix = "a!"
Dblue = "<a:ab_BlueDiamond:938850305083314207>"
Dyellow = "<a:ab_YellowDiamond:938857668888645673>"
Dred = "<a:ab_RedDiamond:938857687788183572>"
Dgreen = "<a:ab_GreenDiamond:938880803692240927>"
Dorange = "<a:ab_OrangeDiamond:938881688866553868>"
Dpink = "<a:ab_PinkDiamond:938882959472205874>"
Dpurple = "<a:ab_PurpleDiamond:938883672717787196>"
Dgray = "<a:ab_GrayDiamond:938884683771543572>"

class cog_div(commands.Cog):
    def __init__(self, bot):
        self.bot = bot

    @commands.group(invoke_without_command=True)
    async def help(self, ctx):
        #with open('prefixes.json', 'r') as f:
        #    prefixes = json.load(f)
        #prefix = prefixes[str(ctx.guild.id)]
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        embed = discord.Embed(description = f"<:NarutoPaint:819963300389453874> • Oi {ctx.author.mention}, eu sou o **AnyBot**. Estou aqui para divertir você(s). Meu prefixo padrão é `a!`, e meu prefixo neste servidor é `{command_prefix}`\n<:SakuraPaint:820513193260089365> • Gostaria de sugerir algum comando para mim? Entre em contato com o meu criador: `Eric2605#9133`\n<:ShikamaruPaint:820479198211997716> • Atualmente eu tenho **125** comandos. Digite `{command_prefix}comandos` para ver os meus comandos.",color = 0x2dffe7)
        embed.set_author(name = f"Central de Ajuda do {self.bot.user.name}", icon_url=self.bot.user.avatar_url)
        embed.set_footer(text="• Para obter informações de cada comando, digite a!help <comando>")
        embed.add_field(name="Categorias:", value="```fix\nMod - Fun - NSFW - Util - Photoshop - Diversos - Jogos\n```", inline=True)
        embed.add_field(name="Extras:", value="**[Meu servidor](https://discord.gg/77ax3PyXgn) | [Canal YT](https://www.youtube.com/channel/UCoo5WAMn4tMl-b0lW0KL9Ug) | [Vote](https://top.gg/bot/900346730237820939/vote)**", inline=False)
        embed.set_thumbnail(url=self.bot.user.avatar_url)
        await ctx.reply(embed=embed)
    @help.command()
    async def mod(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        embed = discord.Embed(title = f"Central de Ajuda - Mod 『{Dblue}』",description = f"#️⃣ **• Nome:** `mod`\n🔍 **• Descrição:** `Mostra uma lista com os comandos de moderação`\n⚙ **• Uso:** `{command_prefix}mod`",color = 0x2c28ff)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/Zyaj8U0.gif")
        await ctx.reply(embed=embed)
    @help.command(aliases = ["diversão","diversao"])
    async def fun(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        embed = discord.Embed(title = f"Central de Ajuda - Fun 『{Dyellow}』",description = f"#️⃣ **• Nome:** `fun`\n🔍 **• Descrição:** `Mostra uma lista com os comandos de diversão`\n⚙ **• Uso:** `{command_prefix}fun`\n🔀 **• Sinônimos:** `{command_prefix}diversão`",color = 0xffbb00)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/hEgd2tI.gif")
        await ctx.reply(embed=embed)
    @help.command()
    async def nsfw(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        embed = discord.Embed(title = f"Central de Ajuda - NSFW 『{Dred}』",description = f"#️⃣ **• Nome:** `nsfw`\n🔍 **• Descrição:** `Mostra uma lista com os comandos de NSFW`\n⚙ **• Uso:** `{command_prefix}nsfw`",color = 0xb51010)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/7H95z1T.gif")
        await ctx.reply(embed=embed)
    @help.command()
    async def util(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        embed = discord.Embed(title = f"Central de Ajuda - Útil 『{Dgreen}』",description = f"#️⃣ **• Nome:** `util`\n🔍 **• Descrição:** `Mostra uma lista com os comandos de utilidades`\n⚙ **• Uso:** `{command_prefix}util`",color = 0x308020)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/heRA5YW.gif")
        await ctx.reply(embed=embed)
    @help.command(aliases = ["ps"])
    async def photoshop(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        embed = discord.Embed(title = f"Central de Ajuda - Photoshop 『{Dorange}』",description = f"#️⃣ **• Nome:** `photoshop`\n🔍 **• Descrição:** `Mostra uma lista com os comandos de Photoshop`\n⚙ **• Uso:** `{command_prefix}photoshop`\n🔀 **• Sinônimos:** `{command_prefix}ps`",color = 0xff7b00)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/A9X6IKH.gif")
        await ctx.reply(embed=embed)
    @help.command()
    async def diversos(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        embed = discord.Embed(title = f"Central de Ajuda - Diversos 『{Dpink}』",description = f"#️⃣ **• Nome:** `diversos`\n🔍 **• Descrição:** `Mostra uma lista com os comandos diversos`\n⚙ **• Uso:** `{command_prefix}diversos`",color = 0xe61ec8)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/oI4uuB3.gif")
        await ctx.reply(embed=embed)
    @help.command(aliases = ["economy","economia"])
    async def games(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        embed = discord.Embed(title = f"Central de Ajuda - Economia 『{Dpurple}』",description = f"#️⃣ **• Nome:** `economy`\n🔍 **• Descrição:** `Mostra uma lista com os comandos de economia`\n⚙ **• Uso:** `{command_prefix}economy`\n🔀 **• Sinônimos:** `{command_prefix}economia, {command_prefix}games`",color = 0x9910ee)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/7yDceNn.gif")
        await ctx.reply(embed=embed)
    @help.command(aliases = ["future"])
    async def futuro(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        embed = discord.Embed(title = f"Central de Ajuda - Futuro 『{Dgray}』",description = f"#️⃣ **• Nome:** `futuro`\n🔍 **• Descrição:** `Mostra uma lista com os futuros comandos do bot`\n⚙ **• Uso:** `{command_prefix}futuro`",color = 0xdddddd)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/hzWmW2D.gif")
        await ctx.reply(embed=embed)
#MOD - Mod - mod
    @help.command(aliases=["createemoji","criaremoji"])
    async def addemoji(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        embed = discord.Embed(title = f"Central de Ajuda - Addemoji 『{Dblue}』",description = f"#️⃣ **• Nome:** `addemoji`\n🔍 **• Descrição:** `Adiciona um emoji`\n⚙ **• Uso:** `{command_prefix}addemoji <link da imagem> <nome do emoji>`\n🔀 **• Sinônimos:** `{command_prefix}criaremoji, {command_prefix}createemoji`\n⚠ **• Avisos:** `O nome do emoji não pode conter espaços e/ou caracteres inválidos! Apenas letras, números e underscores (_) são válidos.`",color = 0x2c28ff)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/jWYywEj.gif")
        await ctx.send(embed=embed)
    @help.command(aliases=["createrole"])
    async def addrole(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        embed = discord.Embed(title = f"Central de Ajuda - Addrole 『{Dblue}』",description = f"#️⃣ **• Nome:** `addrole`\n🔍 **• Descrição:** `Cria um cargo`\n⚙ **• Uso:** `{command_prefix}addrole <nome do cargo>`\n🔀 **• Sinônimos:** `{command_prefix}createrole`",color = 0x2c28ff)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/jWYywEj.gif")
        await ctx.send(embed=embed)
    @help.command(aliases = ["banir"])
    async def ban(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        embed = discord.Embed(title = f"Central de Ajuda - Ban 『{Dblue}』",description = f"#️⃣ **• Nome:** `ban`\n🔍 **• Descrição:** `Bane um usuário`\n⚙ **• Uso:** `{command_prefix}ban <usuário> <motivo>`\n🔀 **• Sinônimos:** `{command_prefix}banir`",color = 0x2c28ff)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/jWYywEj.gif")
        await ctx.send(embed=embed)
    @help.command(aliases=["addbanword","addbannedword"])
    async def banword(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        embed = discord.Embed(title = "Central de Ajuda - Ban Word",description = f"#️⃣ **• Nome:** `banword`\n🔍 **• Descrição:** `Adiciona uma palavra à lista de palavras banidas`\n⚙ **• Uso:** `{command_prefix}banword <palavra>`\n🔀 **• Sinônimos:** `{command_prefix}addbanword, {command_prefix}addbannedword`",color = 0x2c28ff)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/jWYywEj.gif")
        await ctx.send(embed=embed)
    @help.command(aliases = ["expulsar"])
    async def kick(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        embed = discord.Embed(title = f"Central de Ajuda - Kick 『{Dblue}』",description = f"#️⃣ **• Nome:** `kick`\n🔍 **• Descrição:** `Expulsa um usuário`\n⚙ **• Uso:** `{command_prefix}kick <usuário> <motivo>`\n🔀 **• Sinônimos:** `{command_prefix}expulsar`",color = 0x2c28ff)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/jWYywEj.gif")
        await ctx.send(embed=embed)
    @help.command(aliases = ["purge","delete"])
    async def clear(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        embed = discord.Embed(title = f"Central de Ajuda - Clear 『{Dblue}』",description = f"#️⃣ **• Nome:** `clear`\n🔍 **• Descrição:** `Apaga mensagens automaticamente. Podem ser limpas de 1 a 1000 mensagens`\n⚙ **• Uso:** `{command_prefix}clear <número de mensagens>`\n🔀 **• Sinônimos:** `{command_prefix}delete, {command_prefix}purge`",color = 0x2c28ff)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/jWYywEj.gif")
        await ctx.send(embed=embed)
    @help.command(aliases = ["clonechannel","channelclone","clonar"])
    async def clone(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        embed = discord.Embed(title = f"Central de Ajuda - Clone 『{Dblue}』",description = f"#️⃣ **• Nome:** `clone`\n🔍 **• Descrição:** `Clona um canal com o mesmo nome e posição`\n⚙ **• Uso:** `{command_prefix}clone <canal>`\n🔀 **• Sinônimos:** `{command_prefix}clonechannel, {command_prefix}channelclone, {command_prefix}clonar`",color = 0x2c28ff)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/jWYywEj.gif")
        await ctx.send(embed=embed)
    @help.command(aliases = ["channelcreate","addchannel","channeladd"])
    async def createchannel(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        embed = discord.Embed(title = f"Central de Ajuda - Create Channel 『{Dblue}』",description = f"#️⃣ **• Nome:** `createchannel`\n🔍 **• Descrição:** `Cria um canal`\n⚙ **• Uso:** `{command_prefix}createchannel <nome>`\n🔀 **• Sinônimos:** `{command_prefix}channelcreate, {command_prefix}addchannel, {command_prefix}channeladd`",color = 0x2c28ff)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/jWYywEj.gif")
        await ctx.send(embed=embed)
    @help.command(aliases = ["invite","convite"])
    async def createinvite(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        embed = discord.Embed(title = f"Central de Ajuda - Create Invite 『{Dblue}』",description = f"#️⃣ **• Nome:** `createinvite`\n🔍 **• Descrição:** `Cria um convite`\n⏰ **• Tempos disponíveis:** `s (segundos) - m (minutos) - h (horas) - d (dias)`\n💬 **• Exemplo:** `{command_prefix}createinvite 1d`\n🔀 **• Sinônimos:** `{command_prefix}invite, {command_prefix}convite`",color = 0x2c28ff)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/jWYywEj.gif")
        await ctx.send(embed = embed)
    @help.command(aliases = ["delchannel","channeldelete","removechannel","channelremove"])
    async def deletechannel(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        embed = discord.Embed(title = f"Central de Ajuda - Delete Channel 『{Dblue}』",description = f"#️⃣ **• Nome:** `deletechannel`\n🔍 **• Descrição:** `Exclui um canal`\n⚙ **• Uso:** `{command_prefix}deletechannel <canal>`\n🔀 **• Sinônimos:** `{command_prefix}delchannel, {command_prefix}channeldelete, {command_prefix}removechannel, {command_prefix}channelremove`",color = 0x2c28ff)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/jWYywEj.gif")
        await ctx.send(embed=embed)
    @help.command(aliases = ["invitesdelete","delinvites","removeinvites","invitesremove"])
    async def deleteinvites(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        embed = discord.Embed(title = f"Central de Ajuda - Delete Invites 『{Dblue}』",description = f"#️⃣ **• Nome:** `deleteinvites`\n🔍 **• Descrição:** `Exclui todos os convites`\n⚙️ **• Uso:** `{command_prefix}deleteinvites`\n🔀 **• Sinônimos:** `{command_prefix}invitesdelete, {command_prefix}delinvites, {command_prefix}removeinvites, {command_prefix}invitesremove`\n⚠️ **• Atenção:** `Esta ação é irreversível!`",color = 0x2c28ff)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/jWYywEj.gif")
        await ctx.send(embed = embed)
    @help.command(aliases = ["invitesall","allinvites","convites"])
    async def invites(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        embed = discord.Embed(title = f"Central de Ajuda - Invites 『{Dblue}』",description = f"#️⃣ **• Nome:** `invites`\n🔍 **• Descrição:** `Mostra uma lista com todos os convites`\n⚙️ **• Uso:** `{command_prefix}invites`\n🔀 **• Sinônimos:** `{command_prefix}invitesall, {command_prefix}allinvites, {command_prefix}convites`",color = 0x2c28ff)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/jWYywEj.gif")
        await ctx.send(embed = embed)
    @help.command(aliases = ["banlist"])
    async def listban(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        embed = discord.Embed(title = f"Central de Ajuda - List Ban 『{Dblue}』",description = f"#️⃣ **• Nome:** `listban`\n🔍 **• Descrição:** `Mostra a lista de usuários banidos`\n⚙ **• Uso:** `{command_prefix}listban`\n🔀 **• Sinônimos:** `{command_prefix}banlist`",color = 0x2c28ff)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/jWYywEj.gif")
        await ctx.send(embed=embed)
    @help.command()
    async def lock(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        embed = discord.Embed(title = f"Central de Ajuda - Lock 『{Dblue}』",description = f"#️⃣ **• Nome:** `lock`\n🔍 **• Descrição:** `Bloqueia o canal`\n⚙ **• Uso:** `{command_prefix}lock`",color = 0x2c28ff)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/jWYywEj.gif")
        await ctx.send(embed=embed)
    @help.command(aliases = ["silenciar","mutar"])
    async def mute(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        embed = discord.Embed(title = f"Central de Ajuda - Mute 『{Dblue}』",description = f"#️⃣ **• Nome:** `mute`\n🔍 **• Descrição:** `Silencia um usuário`\n⏰ **• Tempos disponíveis:** `s (segundos) - m (minutos) - h (horas)`\n💬 **• Exemplo:** `{command_prefix}mute <usuário> 1m`\n🔀 **• Sinônimos:** `{command_prefix}mutar, {command_prefix}silenciar`",color = 0x2c28ff)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/jWYywEj.gif")
        await ctx.send(embed=embed)
    @help.command(aliases = ["nukechannel","channelnuke","resetchannel"])
    async def nuke(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        embed = discord.Embed(title = f"Central de Ajuda - Nuke 『{Dblue}』",description = f"#️⃣ **• Nome:** `nuke`\n🔍 **• Descrição:** `Cria um novo canal com o mesmo nome e posição, e deleta o antigo`\n⚙ **• Uso:** `{command_prefix}nuke <canal>`\n🔀 **• Sinônimos:** `{command_prefix}nukechannel, {command_prefix}channelnuke, {command_prefix}resetchannel`",color = 0x2c28ff)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/jWYywEj.gif")
        await ctx.send(embed=embed)
    @help.command(aliases = ["deleteemoji","removeremoji","removemoji"])
    async def removeemoji(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        embed = discord.Embed(title = f"Central de Ajuda - Remove Emoji 『{Dblue}』",description = f"#️⃣ **• Nome:** `removeemoji`\n🔍 **• Descrição:** `Exclui um emoji`\n⚙ **• Uso:** `{command_prefix}removeemoji <emoji>`\n🔀 **• Sinônimos:** `{command_prefix}deleteemoji, {command_prefix}removeremoji, {command_prefix}removemoji`",color = 0x2c28ff)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/jWYywEj.gif")
        await ctx.send(embed=embed)
    @help.command(aliases = ["deleterole","delrole"])
    async def removerole(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        embed = discord.Embed(title = f"Central de Ajuda - Remove Role 『{Dblue}』",description = f"#️⃣ **• Nome:** `removerole`\n🔍 **• Descrição:** `Exclui um cargo`\n⚙ **• Uso:** `{command_prefix}removerole <cargo>`\n🔀 **• Sinônimos:** `{command_prefix}deleterole, {command_prefix}delrole`",color = 0x2c28ff)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/jWYywEj.gif")
        await ctx.send(embed=embed)
    @help.command(aliases = ["changechannelname","channelname","namechannel"])
    async def renamechannel(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        embed = discord.Embed(title = f"Central de Ajuda - Rename Channel 『{Dblue}』",description = f"#️⃣ **• Nome:** `renamechannel`\n🔍 **• Descrição:** `Renomeia um canal`\n⚙ **• Uso:** `{command_prefix}renamechannel <canal> <novo nome>`\n🔀 **• Sinônimos:** `{command_prefix}changechannelname, {command_prefix}channelname, {command_prefix}namechannel`",color = 0x2c28ff)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/jWYywEj.gif")
        await ctx.send(embed=embed)
    @help.command()
    async def say(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        embed = discord.Embed(title = f"Central de Ajuda - Say 『{Dblue}』",description = f"#️⃣ **• Nome:** `say`\n🔍 **• Descrição:** `Faz o bot dizer uma mensagem`\n⚙ **• Uso:** `{command_prefix}say <mensagem>`",color = 0x2c28ff)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/jWYywEj.gif")
        await ctx.send(embed=embed)
    @help.command(aliases = ["embedsay"])
    async def sayembed(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        embed = discord.Embed(title = f"Central de Ajuda - Say Embed 『{Dblue}』",description = f"#️⃣ **• Nome:** `sayembed`\n🔍 **• Descrição:** `Faz o bot dizer uma mensagem em Embed`\n⚙ **• Uso:** `{command_prefix}sayembed <mensagem>`\n🔀 **• Sinônimos:** `{command_prefix}embedsay`",color = 0x2c28ff)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/jWYywEj.gif")
        await ctx.send(embed=embed)
    @help.command(aliases = ["changeguildicon"])
    async def setguildicon(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        embed = discord.Embed(title = f"Central de Ajuda - Set Guild Icon 『{Dblue}』",description = f"#️⃣ **• Nome:** `setguildicon`\n🔍 **• Descrição:** `Altera o ícone do servidor`\n⚙ **• Uso:** `{command_prefix}setguildicon <link>`\n🔀 **• Sinônimos:** `{command_prefix}changeguildicon`",color = 0x2c28ff)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/jWYywEj.gif")
        await ctx.send(embed=embed)
    @help.command(aliases = ["changeguildname"])
    async def setguildname(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        embed = discord.Embed(title = f"Central de Ajuda - Set Guild Name 『{Dblue}』",description = f"#️⃣ **• Nome:** `setguildname`\n🔍 **• Descrição:** `Altera o nome do servidor`\n⚙ **• Uso:** `{command_prefix}setguildname <nome>`\n🔀 **• Sinônimos:** `{command_prefix}changeguildname`",color = 0x2c28ff)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/jWYywEj.gif")
        await ctx.send(embed=embed)
    @help.command(aliases = ["setusernick","setnickuser","setusername","usernameset","changenick","nickchange"])
    async def setnick(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        embed = discord.Embed(title = f"Central de Ajuda - Set Nick 『{Dblue}』",description = f"#️⃣ **• Nome:** `setnick`\n🔍 **• Descrição:** `Altera o apelido de um usuário`\n⚙ **• Uso:** `{command_prefix}setnick <usuário> <nome>`\n🔀 **• Sinônimos:** `{command_prefix}setusernick, {command_prefix}setnickuser, {command_prefix}setusername, {command_prefix}usernameset, {command_prefix}changenick, {command_prefix}nickchange`",color = 0x2c28ff)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/jWYywEj.gif")
        await ctx.send(embed=embed)
    @help.command(aliases = ["prefixset","changeprefix"])
    async def setprefix(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        embed = discord.Embed(title = "Central de Ajuda - Set Prefix",description = f"#️⃣ **• Nome:** `setprefix`\n🔍 **• Descrição:** `Altera o prefixo do bot`\n⚙ **• Uso:** `{command_prefix}setprefix <prefixo>`\n⚙ **• Sinônimos:** `{command_prefix}prefixset, {command_prefix}changeprefix`",color = 0x2c28ff)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/jWYywEj.gif")
        await ctx.send(embed=embed)
    @help.command(aliases = ["modolento"])
    async def slowmode(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        embed = discord.Embed(title = f"Central de Ajuda - Slow Mode 『{Dblue}』",description = f"#️⃣ **• Nome:** `slowmode`\n🔍 **• Descrição:** `Define o modo lento de um canal`\n⏰ **• Tempos disponíveis:** `s (segundos) - m (minutos) - h (horas)`\n💬 **• Exemplo:** `{command_prefix}slowmode 1m`\n🔀 **• Sinônimos:** `{command_prefix}modolento`",color = 0x2c28ff)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/jWYywEj.gif")
        await ctx.send(embed=embed)
    @help.command(aliases = ["desbanir","delban","deleteban"])
    async def unban(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        embed = discord.Embed(title = f"Central de Ajuda - Unban 『{Dblue}』",description = f"#️⃣ **• Nome:** `unban`\n🔍 **• Descrição:** `Desbane um usuário`\n⚙ **• Uso:** `{command_prefix}unban <ID do usuário>`\n🔀 **• Sinônimos:** `{command_prefix}desbanir, {command_prefix}deleteban, {command_prefix}delban`",color = 0x2c28ff)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/jWYywEj.gif")
        await ctx.send(embed=embed)
    @help.command(aliases = ["removebannedword","removebanword","deletebannedword","deletebanword","delbannedword","delbanword"])
    async def unbanword(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        embed = discord.Embed(title = "Central de Ajuda - Unban Word",description = f"#️⃣ **• Nome:** `unbanword`\n🔍 **• Descrição:** `Remove uma palavra da lista de palavras banidas`\n⚙ **• Uso:** `{command_prefix}unbanword <palavra>`\n🔀 **• Sinônimos:** `{command_prefix}removebannedword, {command_prefix}removebanword, {command_prefix}deletebannedword, {command_prefix}deletebanword, {command_prefix}delbannedword, {command_prefix}delbanword`",color = 0x2c28ff)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/jWYywEj.gif")
        await ctx.send(embed=embed)
    @help.command()
    async def unlock(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        embed = discord.Embed(title = f"Central de Ajuda - Unlock 『{Dblue}』",description = f"#️⃣ **• Nome:** `unlock`\n🔍 **• Descrição:** `Desbloqueia o canal`\n⚙ **• Uso:** `{command_prefix}unlock`",color = 0x2c28ff)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/jWYywEj.gif")
        await ctx.send(embed=embed)
    @help.command(aliases = ["desmutar","desilenciar"])
    async def unmute(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        embed = discord.Embed(title = f"Central de Ajuda - Unmute 『{Dblue}』",description = f"#️⃣ **• Nome:** `unmute`\n🔍 **• Descrição:** `Desmuta um usuário`\n⚙ **• Uso:** `{command_prefix}unmute <usuário>`\n🔀 **• Sinônimos:** `{command_prefix}desmutar, {command_prefix}desilenciar`",color = 0x2c28ff)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/jWYywEj.gif")
        await ctx.send(embed=embed)
#FUN - Fun - fun
    @help.command()
    async def akumanomi(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        embed = discord.Embed(title = f"Central de Ajuda - Akumanomi 『{Dyellow}』",description = f"#️⃣ **• Nome:** `akumanomi`\n🔍 **• Descrição:** `Mostra uma Akuma no Mi aleatória`\n⚙ **• Uso:** `{command_prefix}akumanomi`",color = 0xffbb00)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/fvDaJc6.gif")
        await ctx.send(embed=embed)
    @help.command(aliases = ["sus","amongus"])
    async def amogus(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        embed = discord.Embed(title = f"Central de Ajuda - Amogus 『{Dyellow}』",description = f"#️⃣ **• Nome:** `amogus`\n🔍 **• Descrição:** `Manda um amogus de emojis`\n⚙ **• Uso:** `{command_prefix}amogus`\n🔀 **• Sinônimos:** `{command_prefix}sus, {command_prefix}amongus`",color = 0xffbb00)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/fvDaJc6.gif")
        await ctx.send(embed=embed)
    @help.command(aliases = ["raiva","rage"])
    async def angry(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        embed = discord.Embed(title = f"Central de Ajuda - Angry 『{Dyellow}』",description = f"#️⃣ **• Nome:** `angry`\n🔍 **• Descrição:** `Fica com raiva`\n⚙ **• Uso:** `{command_prefix}angry`\n🔀 **• Sinônimos:** `{command_prefix}raiva, {command_prefix}rage`",color = 0xffbb00)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/fvDaJc6.gif")
        await ctx.send(embed=embed)
    @help.command(aliases = ["atacar"])
    async def attack(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        embed = discord.Embed(title = f"Central de Ajuda - Attack 『{Dyellow}』",description = f"#️⃣ **• Nome:** `attack`\n🔍 **• Descrição:** `Ataca um usuário`\n⚙ **• Uso:** `{command_prefix}attack <usuário>`\n🔀 **• Sinônimos:** `{command_prefix}ataque`",color = 0xffbb00)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/fvDaJc6.gif")
        await ctx.send(embed=embed)
    @help.command()
    async def baka(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        embed = discord.Embed(title = f"Central de Ajuda - Baka 『{Dyellow}』",description = f"#️⃣ **• Nome:** `baka`\n🔍 **• Descrição:** `Chame o seu amigo de 'idiota' em japonês`\n⚙ **• Uso:** `{command_prefix}baka <usuário>`",color = 0xffbb00)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/fvDaJc6.gif")
        await ctx.send(embed=embed)
    @help.command(aliases = ["punch"])
    async def bater(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        embed = discord.Embed(title = f"Central de Ajuda - Punch 『{Dyellow}』",description = f"#️⃣ **• Nome:** `punch`\n🔍 **• Descrição:** `Bata em um usuário`\n⚙ **• Uso:** `{command_prefix}punch <usuário>`\n🔀 **• Sinônimos:** `{command_prefix}bater`",color = 0xffbb00)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/fvDaJc6.gif")
        await ctx.send(embed=embed)
    @help.command(aliases = ["kiss"])
    async def beijar(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        embed = discord.Embed(title = f"Central de Ajuda - Kiss 『{Dyellow}』",description = f"#️⃣ **• Nome:** `kiss`\n🔍 **• Descrição:** `Beije um usuário`\n⚙ **• Uso:** `{command_prefix}kiss <usuário>`\n🔀 **• Sinônimos:** `{command_prefix}beijar`",color = 0xffbb00)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/fvDaJc6.gif")
        await ctx.send(embed=embed)
    @help.command(aliases = ["morder","mordida"])
    async def bite(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        embed = discord.Embed(title = f"Central de Ajuda - Bite 『{Dyellow}』",description = f"#️⃣ **• Nome:** `bite`\n🔍 **• Descrição:** `Morde um usuário`\n⚙ **• Uso:** `{command_prefix}bite <usuário>`\n🔀 **• Sinônimos:** `{command_prefix}morder, {command_prefix}mordida`",color = 0xffbb00)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/fvDaJc6.gif")
        await ctx.send(embed=embed)
    @help.command(aliases = ["tchau"])
    async def bye(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        embed = discord.Embed(title = f"Central de Ajuda - Bye 『{Dyellow}』",description = f"#️⃣ **• Nome:** `bye`\n🔍 **• Descrição:** `Se despede de um usuário`\n⚙ **• Uso:** `{command_prefix}bye <usuário>`\n🔀 **• Sinônimos:** `{command_prefix}tchau`",color = 0xffbb00)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/fvDaJc6.gif")
        await ctx.send(embed=embed)
    @help.command(aliases = ["pat"])
    async def cafune(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        embed = discord.Embed(title = f"Central de Ajuda - Cafune 『{Dyellow}』",description = f"#️⃣ **• Nome:** `cafune`\n🔍 **• Descrição:** `Faça um cafuné em um usuário`\n⚙ **• Uso:** `{command_prefix}cafune <usuário>`\n🔀 **• Sinônimos:** `{command_prefix}pat`",color = 0xffbb00)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/fvDaJc6.gif")
        await ctx.send(embed=embed)
    @help.command()
    async def cantada(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        embed = discord.Embed(title = f"Central de Ajuda - Cantada 『{Dyellow}』",description = f"#️⃣ **• Nome:** `cantada`\n🔍 **• Descrição:** `Manda uma cantada`\n⚙ **• Uso:** `{command_prefix}cantada`",color = 0xffbb00)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/fvDaJc6.gif")
        await ctx.send(embed=embed)
    @help.command(aliases = [])
    async def casar(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        embed = discord.Embed(title = "Central de Ajuda - Casar",description = f"#️⃣ **• Nome:** `casar`\n🔍 **• Descrição:** `Case-se com um usuário`\n⚙ **• Uso:** `{command_prefix}casar <usuário>`\n🔀 **• Sinônimos:** `{command_prefix}marry`",color = 0xffbb00)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/fvDaJc6.gif")
        await ctx.send(embed=embed)
    @help.command(aliases = ["cheek","bochecha"])
    async def cheeks(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        embed = discord.Embed(title = f"Central de Ajuda - Cheeks 『{Dyellow}』",description = f"#️⃣ **• Nome:** `cheeks`\n🔍 **• Descrição:** `Brinca com as bochechas de um usuário`\n⚙ **• Uso:** `{command_prefix}cheeks <usuário>`\n🔀 **• Sinônimos:** `{command_prefix}cheek, {command_prefix}bochecha`",color = 0xffbb00)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/fvDaJc6.gif")
        await ctx.send(embed=embed)
    @help.command()
    async def dado(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        embed = discord.Embed(title = f"Central de Ajuda - Dado 『{Dyellow}』",description = f"#️⃣ **• Nome:** `dado`\n🔍 **• Descrição:** `Jogue um dado de 6 lados`\n⚙ **• Uso:** `{command_prefix}dado`\n⭐**• Extra:** `Para jogar 2 dados, digite '{command_prefix}dados2'`",color = 0xffbb00)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/fvDaJc6.gif")
        await ctx.send(embed=embed)
    @help.command()
    async def dick(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        embed = discord.Embed(title = f"Central de Ajuda - Dick 『{Dyellow}』",description = f"#️⃣ **• Nome:** `dick`\n🔍 **• Descrição:** `Veja o tamanho do seu amigo🐥`\n⚙ **• Uso:** `{command_prefix}dick`",color = 0xffbb00)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/fvDaJc6.gif")
        await ctx.send(embed=embed)
    @help.command()
    async def ednaldo(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        embed = discord.Embed(title = "Central de Ajuda - Ednaldo",description = f"#️⃣ **• Nome:** `ednaldo`\n🔍 **• Descrição:** `Mensagem do Ednaldo Pereira`\n⚙ **• Uso:** `{command_prefix}ednaldo`\n🔀 **• Sinônimos:** `{command_prefix}ednaldopereira`",color = 0xffbb00)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/fvDaJc6.gif")
        await ctx.send(embed=embed)
    @help.command()
    async def emojify(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        embed = discord.Embed(title = "Central de Ajuda - Emojify",description = f"#️⃣ **• Nome:** `emojify`\n🔍 **• Descrição:** `Transforma mensagens de texto em emojis`\n⚙ **• Uso:** `{command_prefix}emojify <texto>`",color = 0xffbb00)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/fvDaJc6.gif")
        await ctx.send(embed=embed)
    @help.command(aliases = ["fakeprofile"])
    async def fake(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        embed = discord.Embed(title = "Central de Ajuda - Fake",description = f"#️⃣ **• Nome:** `fake`\n🔍 **• Descrição:** `Cria uma mensagem falsa de um usuário`\n⚙ **• Uso:** `{command_prefix}fake <usuário> <mensagem>`\n🔀 **• Sinônimos:** `{command_prefix}fakeprofile, {command_prefix}fp`\n💬 **• Exemplo:** `a!fake `{ctx.author.mention}` Esta é uma mensagem fake!`",color = 0xffbb00)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/fvDaJc6.gif")
        await ctx.send(embed=embed)
    @help.command()
    async def foto(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        embed = discord.Embed(title = "Central de Ajuda - Foto",description = f"#️⃣ **• Nome:** `foto`\n🔍 **• Descrição:** `Mostra uma imagem aleatória. (Em manutenção)`\n⚙ **• Uso:** `{command_prefix}foto`",color = 0xffbb00)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/fvDaJc6.gif")
        await ctx.send(embed=embed)
    @help.command()
    async def hack(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        embed = discord.Embed(title = f"Central de Ajuda - Hack 『{Dyellow}』",description = f"#️⃣ **• Nome:** `hack`\n🔍 **• Descrição:** `\"Hackeia\" um usuário`\n⚙ **• Uso:** `{command_prefix}hack <usuário>`",color = 0xffbb00)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/fvDaJc6.gif")
        await ctx.send(embed=embed)
    @help.command(aliases = ["abraço","abraçar"])
    async def hug(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        embed = discord.Embed(title = f"Central de Ajuda - Hug 『{Dyellow}』",description = f"#️⃣ **• Nome:** `hug`\n🔍 **• Descrição:** `Abrace um usuário`\n⚙ **• Uso:** `{command_prefix}hug <usuário>`\n🔀 **• Sinônimos:** `{command_prefix}abraço, {command_prefix}abraçar`",color = 0xffbb00)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/fvDaJc6.gif")
        await ctx.send(embed=embed)
    @help.command()
    async def impostor(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        embed = discord.Embed(title = f"Central de Ajuda - Impostor 『{Dyellow}』",description = f"#️⃣ **• Nome:** `impostor`\n🔍 **• Descrição:** `Mostra quem é o impostor do servidor`\n⚙ **• Uso:** `{command_prefix}impostor`",color = 0xffbb00)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/fvDaJc6.gif")
        await ctx.send(embed=embed)
    @help.command()
    async def moeda(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        embed = discord.Embed(title = f"Central de Ajuda - Moeda 『{Dyellow}』",description = f"#️⃣ **• Nome:** `moeda`\n🔍 **• Descrição:** `Jogue Cara ou Coroa`\n⚙ **• Uso:** `{command_prefix}moeda`",color = 0xffbb00)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/fvDaJc6.gif")
        await ctx.send(embed=embed)
    @help.command(aliases = ["ora","muda"])
    async def jjbattle(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        embed = discord.Embed(title = "Central de Ajuda - JJBattle",description = f"#️⃣ **• Nome:** `jjbattle`\n🔍 **• Descrição:** `Desafie um usuário a uma batalha estilo Jojo`\n⚙ **• Uso:** `{command_prefix}jjbattle <usuário>`\n🔀 **• Sinônimos:** `{command_prefix}ora, {command_prefix}muda`",color = 0xffbb00)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/fvDaJc6.gif")
        await ctx.send(embed=embed)
    @help.command()
    async def nitro(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        embed = discord.Embed(title = "Central de Ajuda - Nitro",description = f"#️⃣ **• Nome:** `nitro`\n🔍 **• Descrição:** `Da um nitro grátis e 100% verídico`\n⚙ **• Uso:** `{command_prefix}nitro`\n⚠ **• Aviso:** `Não é um vírus`",color = 0xffbb00)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/fvDaJc6.gif")
        await ctx.send(embed=embed)
    @help.command(aliases = ["8ball"])
    async def pergunta(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        embed = discord.Embed(title = f"Central de Ajuda - 8ball 『{Dyellow}』",description = f"#️⃣ **• Nome:** `8ball`\n🔍 **• Descrição:** `Faça uma resposta e responderei com 'Sim' ou 'Não'`\n⚙ **• Uso:** `{command_prefix}8ball <pergunta>`\n🔀 **• Sinônimos:** `{command_prefix}pergunta`",color = 0xffbb00)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/fvDaJc6.gif")
        await ctx.send(embed=embed)
    @help.command(aliases=["senseiraylamm","mestreraylamm","railam","senseirailam","mestrerailam","raylam","senseiraylam","mestreraylam"])
    async def raylamm(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        embed = discord.Embed(title = "Central de Ajuda - Raylamm",description = f"#️⃣ **• Nome:** `raylamm`\n🔍 **• Descrição:** `Dica do Grão Mestre Raylamm`\n⚙ **• Uso:** `{command_prefix}raylamm`\n🔀 **• Sinônimos:** `{command_prefix}senseiraylamm, {command_prefix}mestreraylamm, {command_prefix}railam, {command_prefix}senseirailam, {command_prefix}mestrerailam, {command_prefix}raylam, {command_prefix}senseiraylam, {command_prefix}mestreraylam`",color = 0xffbb00)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/fvDaJc6.gif")
        await ctx.send(embed=embed)
    @help.command(aliases = ["tapa"])
    async def slap(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        embed = discord.Embed(title = f"Central de Ajuda - Slap 『{Dyellow}』",description = f"#️⃣ **• Nome:** `slap`\n🔍 **• Descrição:** `Dê um tapa em um usuário`\n⚙ **• Uso:** `{command_prefix}slap <usuário>`\n🔀 **• Sinônimos:** `{command_prefix}tapa`",color = 0xffbb00)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/fvDaJc6.gif")
        await ctx.send(embed=embed)
    @help.command()
    async def ship(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        embed = discord.Embed(title = "Central de Ajuda - Ship",description = f"#️⃣ **• Nome:** `ship`\n🔍 **• Descrição:** `Shippe você e 1 pessoa`\n⚙ **• Uso:** `{command_prefix}ship <usuário>`",color = 0xffbb00)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/fvDaJc6.gif")
        await ctx.send(embed=embed)
    @help.command(aliases = ["ttc","jogodavelha","jdv"])
    async def tictactoe(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        embed = discord.Embed(title = "Central de Ajuda - Tic Tac Toe",description = f"#️⃣ **• Nome:** `tictactoe`\n🔍 **• Descrição:** `Jogue o \'jogo da velha\' com um usuário`\n⚙ **• Uso:** `{command_prefix}tictactoe <usuário>`\n<:ab_intAmarelo:911068181437771887> **• Como jogar:** `Após iniciar a partida, use o comando {command_prefix}place para escolher onde quer jogar! Exemplo: {command_prefix}place 1`\n🔀 **• Sinônimos:** `{command_prefix}ttc, {command_prefix}jogodavelha, {command_prefix}jdv`",color = 0xffbb00)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/fvDaJc6.gif")
        await ctx.send(embed=embed)
    @help.command()
    async def x1(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        embed = discord.Embed(title = "Central de Ajuda - X1",description = f"#️⃣ **• Nome:** `x1`\n🔍 **• Descrição:** `Faça uma batalha com um usuário`\n⚙ **• Uso:** `{command_prefix}x1 <usuário>`\n🔀 **• Sinônimos:** `{command_prefix}lutar, {command_prefix}battle`",color = 0xffbb00)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/fvDaJc6.gif")
        await ctx.send(embed=embed)
#NSFW - Nsfw - nsfw
    @help.command()
    async def anal(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        if ctx.channel.is_nsfw():
            desc = f"#️⃣ **• Nome:** `anal`\n🔍 **• Descrição:** `Manda um imagem/gif de hentai anal`\n⚙ **• Uso:** `{command_prefix}anal`\n⭐ **• Extra:** `É possível fazer um usuário dar a bunda, mencionando um usuário.`"
        else:
            desc = f"🔞 **• **`Este comando só pode ser usado em canais NSFW`"
        embed = discord.Embed(title = f"Central de Ajuda - Anal 『{Dred}』",description = desc,color = 0xb51010)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/Di07Tuw.gif")
        await ctx.send(embed=embed)
    @help.command(aliases = ["fellatio","boquete","mamada"])
    async def blowjob(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        if ctx.channel.is_nsfw():
            desc = f"#️⃣ **• Nome:** `blowjob`\n🔍 **• Descrição:** `Manda um imagem/gif de boquete hentai`\n⚙ **• Uso:** `{command_prefix}blowjob`\n⭐ **• Extra:** `É possível fazer um usuário te pagar um boquete, mencionando um usuário.`\n💬 **• Exemplo:** `{command_prefix}blowjob <usuário>`\n🔀 **• Sinônimos:** `{command_prefix}fellatio, {command_prefix}boquete, {command_prefix}mamada`"
        else:
            desc = f"🔞 **• **`Este comando só pode ser usado em canais NSFW`"
        embed = discord.Embed(title = f"Central de Ajuda - Blowjob 『{Dred}』",description = desc,color = 0xb51010)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/Di07Tuw.gif")
        await ctx.send(embed=embed)
    @help.command(aliases = ["boobjob","titjob","peitos"])
    async def boobs(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        if ctx.channel.is_nsfw():
            desc = f"#️⃣ **• Nome:** `boobs`\n🔍 **• Descrição:** `Manda um imagem/gif de peitos hentai`\n⚙ **• Uso:** `{command_prefix}blowjob`\n⭐ **• Extra:** `É possível fazer um usuário usar peitos em você, mencionando um usuário.`\n💬 **• Exemplo:** `{command_prefix}boobs <usuário>`\n🔀 **• Sinônimos:** `{command_prefix}boobjob, {command_prefix}titjob, {command_prefix}peitos`"
        else:
            desc = f"🔞 **• **`Este comando só pode ser usado em canais NSFW`"
        embed = discord.Embed(title = f"Central de Ajuda - Boobs 『{Dred}』",description = desc,color = 0xb51010)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/Di07Tuw.gif")
        await ctx.send(embed=embed)
    @help.command(aliases = ["lolice"])
    async def loli(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        if ctx.channel.is_nsfw():
            desc = f"#️⃣ **• Nome:** `loli`\n🔍 **• Descrição:** `Manda um meme de loli`\n⚙ **• Uso:** `{command_prefix}loli`\n🔀 **• Sinônimos:** `{command_prefix}lolice`"
        else:
            desc = f"🔞 **• **`Este comando só pode ser usado em canais NSFW`",
        embed = discord.Embed(title = f"Central de Ajuda - Loli 『{Dred}』",description = desc,color = 0xb51010)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/Di07Tuw.gif")
        await ctx.send(embed=embed)
    @help.command(aliases = ["buceta","vagina","bct"])
    async def pussy(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        if ctx.channel.is_nsfw():
            desc = f"#️⃣ **• Nome:** `pussy`\n🔍 **• Descrição:** `Manda um imagem/gif de vagina hentai`\n⚙ **• Uso:** `{command_prefix}pussy`\n⭐ **• Extra:** `É possível fazer um usuário te pagar um boquete, mencionando um usuário.`\n🔀 **• Sinônimos:** `{command_prefix}buceta, {command_prefix}vagina, {command_prefix}bct`"
        else:
            desc = f"🔞 **• **`Este comando só pode ser usado em canais NSFW`"
        embed = discord.Embed(title = f"Central de Ajuda - Pussy 『{Dred}』",description = desc,color = 0xb51010)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/Di07Tuw.gif")
        await ctx.send(embed=embed)
    @help.command(aliases = ["regra34"])
    async def rule34(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        if ctx.channel.is_nsfw():
            desc = f"#️⃣ **• Nome:** `rule34`\n🔍 **• Descrição:** `Faz uma pesquisa de imagem no Rule 34`\n⚙ **• Uso:** `{command_prefix}rule34 <pesquisa>`\n⚠️ **• Aviso:** `Este comando ainda está em manutenção. É possível que algumas pesquisas não mostrem resultados.`\n🔀 **• Sinônimos:** `{command_prefix}regra34`"
        else:
            desc = f"🔞 **• **`Este comando só pode ser usado em canais NSFW`"
        embed = discord.Embed(title = f"Central de Ajuda - Rule34 『{Dred}』",description = desc,color = 0xb51010)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/Di07Tuw.gif")
        await ctx.send(embed=embed)
    @help.command(aliases = ["tapanagostosa"])
    async def spank(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        if ctx.channel.is_nsfw():
            desc = f"#️⃣ **• Nome:** `spank`\n🔍 **• Descrição:** `Da um tapa na bunda de um usuário`\n⚙ **• Uso:** `{command_prefix}spank <usuário>`\n🔀 **• Sinônimos:** `{command_prefix}tapanagostosa`"
        else:
            desc = f"🔞 **• **`Este comando só pode ser usado em canais NSFW`"
        embed = discord.Embed(title = f"Central de Ajuda - Spank 『{Dred}』",description = desc,color = 0xb51010)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/Di07Tuw.gif")
        await ctx.send(embed=embed)
#UTIL - Util - util
    @help.command()
    async def anime(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        embed = discord.Embed(title = f"Central de Ajuda - Anime 『{Dgreen}』",description = f"#️⃣ **• Nome:** `anime`\n🔍 **• Descrição:** `Mostra informações sobre um anime`\n⚙ **• Uso:** `{command_prefix}anime <nome do anime>`",color = 0x308020)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/oWTqwGP.gif")
        await ctx.send(embed=embed)
    @help.command()
    async def avatar(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        embed = discord.Embed(title = f"Central de Ajuda - Avatar 『{Dgreen}』",description = f"#️⃣ **• Nome:** `avatar`\n🔍 **• Descrição:** `Mostra o avatar de um usuário`\n⚙ **• Uso:** `{command_prefix}avatar <usuário>`",color = 0x308020)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/oWTqwGP.gif")
        await ctx.send(embed=embed)
    @help.command()
    async def banner(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        embed = discord.Embed(title = f"Central de Ajuda - Banner 『{Dgreen}』",description = f"#️⃣ **• Nome:** `banner`\n🔍 **• Descrição:** `Mostra o banner de um usuário`\n⚙ **• Uso:** `{command_prefix}banner <usuário>`",color = 0x308020)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/oWTqwGP.gif")
        await ctx.send(embed=embed)
    @help.command(aliases = ["calcular","calcule"])
    async def calc(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        embed = discord.Embed(title = f"Central de Ajuda - Calc 『{Dgreen}』",description = f"#️⃣ **• Nome:** `calc`\n🔍 **• Descrição:** `Faça um cálculo (Em manutenção)`\n⚙ **• Uso:** `{command_prefix}calc <número 1> <operador> <número 2>`\n💬 **• Exemplo:** `{command_prefix}calc 2 + 2`\n🔀 **Sinônimos:** `{command_prefix}calcular, {command_prefix}calcule`",color = 0x308020)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/oWTqwGP.gif")
        await ctx.send(embed=embed)
    @help.command(aliases = ["infochannel","chatinfo"])
    async def channelinfo(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        embed = discord.Embed(title = f"Central de Ajuda - Channel Info 『{Dgreen}』",description = f"#️⃣ **• Nome:** `channelinfo`\n🔍 **• Descrição:** `Mostra as informações de um canal de texto`\n⚙ **• Uso:** `{command_prefix}channelinfo`\n🔀 **• Sinônimos:** `{command_prefix}infochannel, {command_prefix}chatinfo`",color = 0x308020)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/oWTqwGP.gif")
        await ctx.send(embed=embed)
    @help.command(aliases = ["rgb","rbg","grb","gbr","brg","bgr"])
    async def color(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        embed = discord.Embed(title = "Central de Ajuda - Color",description = f"#️⃣ **• Nome:** `color`\n🔍 **• Descrição:** `Mostra um cor em RGB/Hexadecimal`\n⚙ **• Uso:** `{command_prefix}color <R> <G> <B>`\n💬 **• Exemplo:** `{command_prefix}color 0 120 255`\n🔀 **• Sinônimos:** `{command_prefix}rgb`",color = 0x308020)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/oWTqwGP.gif")
        await ctx.send(embed=embed)
    @help.command()
    async def data(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        embed = discord.Embed(title = "Central de Ajuda - Data",description = f"#️⃣ **• Nome:** `data`\n🔍 **• Descrição:** `Mostra a data e o horário local`\n⚙ **• Uso:** `{command_prefix}data`",color = 0x308020)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/oWTqwGP.gif")
        await ctx.send(embed=embed)
    @help.command(aliases = ["emj"])
    async def emoji(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        embed = discord.Embed(title = f"Central de Ajuda - Emoji 『{Dgreen}』",description = f"#️⃣ **• Nome:** `emoji`\n🔍 **• Descrição:** `Veja um emoji em seu tamanho original`\n⚙ **• Uso:** `{command_prefix}emoji <emoji>`\n🔀 **• Sinônimos:** `{command_prefix}emj`",color = 0x308020)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/oWTqwGP.gif")
        await ctx.send(embed=embed)
    @help.command(aliases = ["emojinfo","infoemoji"])
    async def emojiinfo(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        embed = discord.Embed(title = f"Central de Ajuda - Emoji Info 『{Dgreen}』",description = f"#️⃣ **• Nome:** `emojiinfo`\n🔍 **• Descrição:** `Mostra as informações de um emoji customizado`\n⚙ **• Uso:** `{command_prefix}emojiinfo <emoji>`\n🔀 **• Sinônimos:** `{command_prefix}emojinfo, {command_prefix}infoemoji`",color = 0x308020)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/oWTqwGP.gif")
        await ctx.send(embed=embed)
    @help.command(aliases = ["firstmsg","primeiramensagem","primeiramsg"])
    async def firstmessage(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        embed = discord.Embed(title = "Central de Ajuda - First Message",description = f"#️⃣ **• Nome:** `firstmessage`\n🔍 **• Descrição:** `Mostra a primeira mensagem enviada em um canal`\n⚙ **• Uso:** `{command_prefix}firstmessage <canal>`\n🔀 **• Sinônimos:** `{command_prefix}firstmsg, {command_prefix}primeiramensagem, {command_prefix}primeiramsg`",color = 0x308020)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/oWTqwGP.gif")
        await ctx.send(embed=embed)
    @help.command(aliases = ["fonte"])
    async def font(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        embed = discord.Embed(title = "Central de Ajuda - Font",description = f"#️⃣ **• Nome:** `font`\n🔍 **• Descrição:** `Manda uma mensagem em diferentes fontes`\n⚙ **• Uso:** `{command_prefix}font <fonte> <texto>`\n🔀 **• Sinônimos:** `{command_prefix}fonte`\n✍ **• Fontes:** `Para ver todas as fontes, digite {command_prefix}fonts`",color = 0x308020)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/oWTqwGP.gif")
        await ctx.send(embed=embed)
    @help.command(aliases = ["iconguild","guildimage"])
    async def guildicon(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        embed = discord.Embed(title = "Central de Ajuda - Guild Icon",description = f"#️⃣ **• Nome:** `guildicon`\n🔍 **• Descrição:** `Mostra o ícone do servidor`\n⚙ **• Uso:** `{command_prefix}guildicon`\n🔀 **• Sinônimos:** `{command_prefix}iconguild, {command_prefix}guildimage`",color = 0x308020)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/oWTqwGP.gif")
        await ctx.send(embed=embed)
    @help.command(aliases = ["lang","idiomas"])
    async def languages(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        embed = discord.Embed(title = "Central de Ajuda - Languages",description = f"#️⃣ **• Nome:** `languages`\n🔍 **• Descrição:** `Mostra uma lista de todos os idiomas disponíveis no {command_prefix}translate`\n⚙ **• Uso:** `{command_prefix}languages`\n🔀 **• Sinônimos:** `{command_prefix}lang, {command_prefix}idiomas`",color = 0x308020)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/oWTqwGP.gif")
        await ctx.send(embed=embed)
    @help.command(aliases = ["mc","membercount","member_count","members"])
    async def membros(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        embed = discord.Embed(title = "Central de Ajuda - Membros",description = f"#️⃣ **• Nome:** `membros`\n🔍 **• Descrição:** `Mostra a contagem de membros do servidor`\n⚙ **• Uso:** `{command_prefix}membros`\n🔀 **• Sinônimos:** `{command_prefix}mc, {command_prefix}membercount, {command_prefix}member_count, {command_prefix}members`",color = 0x308020)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/oWTqwGP.gif")
        await ctx.send(embed=embed)
    @help.command(aliases = ["ri","inforule"])
    async def roleinfo(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        embed = discord.Embed(title = f"Central de Ajuda - Role Info 『{Dgreen}』",description = f"#️⃣ **• Nome:** `roleinfo`\n🔍 **• Descrição:** `Mostra as informações de um cargo`\n⚙ **• Uso:** `{command_prefix}roleinfo <cargo>`\n🔀 **• Sinônimos:** `{command_prefix}ri, {command_prefix}inforule`",color = 0x308020)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/oWTqwGP.gif")
        await ctx.send(embed=embed)
    @help.command(aliases = ["userrole","userole"])
    async def roleuser(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        embed = discord.Embed(title = "Central de Ajuda - Role User",description = f"#️⃣ **• Nome:** `roleuser`\n🔍 **• Descrição:** `Mostra todos os usuários que possuem um cargo`\n⚙ **• Uso:** `{command_prefix}roleuser <cargo>`\n🔀 **• Sinônimos:** `{command_prefix}userrole, {command_prefix}userole`",color = 0x308020)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/oWTqwGP.gif")
        await ctx.send(embed=embed)
    @help.command(aliases = ["infoserver","guildinfo","si"])
    async def serverinfo(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        embed = discord.Embed(title = f"Central de Ajuda - Server Info 『{Dgreen}』",description = f"#️⃣ **• Nome:** `serverinfo`\n🔍 **• Descrição:** `Mostra as informações do servidor`\n⚙ **• Uso:** `{command_prefix}serverinfo`\n🔀 **• Sinônimos:** `{command_prefix}infoserver, {command_prefix}guildinfo, {command_prefix}si`",color = 0x308020)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/oWTqwGP.gif")
        await ctx.send(embed=embed)
    @help.command(aliases=["cronometro","cronômetro"])
    async def timer(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        embed = discord.Embed(title = "Central de Ajuda - Timer",description = f"#️⃣ **• Nome:** `timer`\n🔍 **• Descrição:** `Define um cronômetro`\n⏰ **• Tempos disponíveis:** `s (segundos) - m (minutos) - h (horas)`\n💬 **• Exemplo:** `{command_prefix}timer 1m`\n🔀 **• Sinônimos:** `{command_prefix}cronômetro, {command_prefix}cronometro`",color = 0x308020)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/oWTqwGP.gif")
        await ctx.send(embed=embed)
    @help.command(aliases = ["traduzir","tradução"])
    async def translate(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        embed = discord.Embed(title = "Central de Ajuda - Translate",description = f"#️⃣ **• Nome:** `translate`\n🔍 **• Descrição:** `Traduz um texto para diferentes idiomas`\n⚙ **• Uso:** `{command_prefix}translate <idioma> <texto>`\n🔀 **• Sinônimos:** `{command_prefix}traduzir, {command_prefix}tradução`\n🗺️ **• Idiomas:** `Para ver todos os idiomas disponíveis, digite {command_prefix}languages`",color = 0x308020)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/oWTqwGP.gif")
        await ctx.send(embed=embed)
    @help.command(aliases = ["infouser","memberinfo"])
    async def userinfo(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        embed = discord.Embed(title = f"Central de Ajuda - User Info 『{Dgreen}』",description = f"#️⃣ **• Nome:** `userinfo`\n🔍 **• Descrição:** `Mostra as informações sobre um usuário`\n⚙ **• Uso:** `{command_prefix}userinfo <usuário>`\n🔀 **• Sinônimos:** `{command_prefix}infouser, {command_prefix}memberinfo`",color = 0x308020)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/oWTqwGP.gif")
        await ctx.send(embed=embed)
    @help.command(aliases = ["roles","useroles"])
    async def userroles(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        embed = discord.Embed(title = f"Central de Ajuda - User Roles 『{Dgreen}』",description = f"#️⃣ **• Nome:** `userroles`\n🔍 **• Descrição:** `Mostra todos os cargos de um usuário`\n⚙ **• Uso:** `{command_prefix}userroles <usuário>`\n🔀 **• Sinônimos:** `{command_prefix}roles, {command_prefix}useroles`",color = 0x308020)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/oWTqwGP.gif")
        await ctx.send(embed=embed)
    @help.command(aliases = ["clima","tempo"])
    async def weather(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        embed = discord.Embed(title = "Central de Ajuda - Weather",description = f"#️⃣ **• Nome:** `wheater`\n🔍 **• Descrição:** `Mostra o clima de alguma cidade`\n⚙ **• Uso:** `{command_prefix}weather <cidade>`\n🔀 **• Sinônimos:** `{command_prefix}clima, {command_prefix}tempo`",color = 0x308020)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/oWTqwGP.gif")
        await ctx.send(embed=embed)
#PHOTOSHOP - Photoshop - photoshop
    @help.command()
    async def bbb22(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        embed = discord.Embed(title = f"Central de Ajuda - BBB 22 『{Dorange}』",description = f"#️⃣ **• Nome:** `bbb22`\n🔍 **• Descrição:** `Cria um participante do BBB 22`\n⚙ **• Uso:** `{command_prefix}bbb22 <usuário>`",color = 0xff7b00)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/U62d9Oo.gif")
        await ctx.send(embed=embed)
    @help.command()
    async def candidato(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        embed = discord.Embed(title = f"Central de Ajuda - Candidato 『{Dorange}』",description = f"#️⃣ **• Nome:** `candidato`\n🔍 **• Descrição:** `Cria um candidato fake com um usuário`\n⚙ **• Uso:** `{command_prefix}candidato <usuário>`",color = 0xff7b00)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/U62d9Oo.gif")
        await ctx.send(embed=embed)
    @help.command(aliases = ["recaptcha"])
    async def captcha(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        embed = discord.Embed(title = "Central de Ajuda - Captcha",description = f"#️⃣ **• Nome:** `captcha`\n🔍 **• Descrição:** `Cria um captcha com o avatar de um usuário`\n⚙ **• Uso:** `{command_prefix}captcha <usuário> <texto>`\n🔀 **• Sinônimos:** `{command_prefix}recaptcha`",color = 0xff7b00)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/U62d9Oo.gif")
        await ctx.send(embed=embed)
    @help.command(aliases = ["giftchristmas","natalpresente","presentenatal","natalgift","giftnatal"])
    async def christmasgift(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        embed = discord.Embed(title = "Central de Ajuda - Christmas Gift 『🎁』",description = f"#️⃣ **• Nome:** `christmasgift`\n🔍 **• Descrição:** `Edita um meme de presente de natal`\n⚙ **• Uso:** `{command_prefix}chirstmasgift <usuário>`\n🔀 **• Sinônimos:** `{command_prefix}giftchristmas, {command_prefix}natalpresente, {command_prefix}presentenatal, {command_prefix}natalgift, {command_prefix}giftnatal`",color = 0xff7b00)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/U62d9Oo.gif")
        await ctx.send(embed=embed)
    @help.command()
    async def clyde(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        embed = discord.Embed(title = "Central de Ajuda - Clyde",description = f"#️⃣ **• Nome:** `clyde`\n🔍 **• Descrição:** `Edita o texto de uma mensagem do bot Clyde`\n⚙ **• Uso:** `{command_prefix}clyde <texto>`",color = 0xff7b00)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/U62d9Oo.gif")
        await ctx.send(embed=embed)
    @help.command(aliases = ["crewmates"])
    async def crewmate(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        embed = discord.Embed(title = "Central de Ajuda - Crewmate",description = f"#️⃣ **• Nome:** `crewmate`\n🔍 **• Descrição:** `Edita a imagem dos tripulantes com membros do servidor`\n⚙ **• Uso:** `{command_prefix}crewmate`\n🔀 **• Sinônimos:** `{command_prefix}crewmates`",color = 0xff7b00)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/U62d9Oo.gif")
        await ctx.send(embed=embed)
    @help.command(aliases = ["facecom"])
    async def facecomment(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        embed = discord.Embed(title = "Central de Ajuda - Face Comment",description = f"#️⃣ **• Nome:** `facecomment`\n🔍 **• Descrição:** `Edita o texto de um comentário do Facebook`\n⚙ **• Uso:** `{command_prefix}facecomment <texto>`\n🔀 **• Sinônimos:** `{command_prefix}facecom`",color = 0xff7b00)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/U62d9Oo.gif")
        await ctx.send(embed=embed)
    @help.command(aliases = ["postface"])
    async def facepost(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        embed = discord.Embed(title = "Central de Ajuda - Face Post",description = f"#️⃣ **• Nome:** `facepost`\n🔍 **• Descrição:** `Cria uma postagem do Facebook`\n⚙ **• Uso:** `{command_prefix}facepost <num> <texto>`\n📚 **• Edições:** Para ver os números e todas os fundos disponíveis, digite apenas `{command_prefix}facepost`",color = 0xff7b00)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/U62d9Oo.gif")
        await ctx.send(embed=embed)
    @help.command(aliases = ["fatos"])
    async def fato(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        embed = discord.Embed(title = "Central de Ajuda - Fato",description = f"#️⃣ **• Nome:** `fato`\n🔍 **• Descrição:** `Edita o texto do meme Fatos do Ei Nerd`\n⚙ **• Uso:** `{command_prefix}fatos <texto>`",color = 0xff7b00)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/U62d9Oo.gif")
        await ctx.send(embed=embed)
    @help.command(aliases = ["hipocrita"])
    async def hipocrisia(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        embed = discord.Embed(title = "Central de Ajuda - Hipocrisia",description = f"#️⃣ **• Nome:** `hipocrisia`\n🔍 **• Descrição:** `Edita o texto do meme \'Enfim, a hipocrisia\'`\n⚙ **• Uso:** `{command_prefix}hipocrisia <texto>`\n🔀 **• Sinônimos:** `{command_prefix}hipocrita`",color = 0xff7b00)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/U62d9Oo.gif")
        await ctx.send(embed=embed)
    @help.command(aliases = ["instacom"])
    async def instacomment(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        embed = discord.Embed(title = "Central de Ajuda - Insta Comment",description = f"#️⃣ **• Nome:** `instacomment`\n🔍 **• Descrição:** `Edita o texto de um comentário do Instagram`\n⚙ **• Uso:** `{command_prefix}instacomment <texto>`\n🔀 **• Sinônimos:** `{command_prefix}instacom`",color = 0xff7b00)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/U62d9Oo.gif")
        await ctx.send(embed=embed)
    @help.command(aliases = ["inverter"])
    async def invert(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        embed = discord.Embed(title = "Central de Ajuda - Invert",description = f"#️⃣ **• Nome:** `invert`\n🔍 **• Descrição:** `Inverte as cores do avatar de um usuário`\n⚙ **• Uso:** `{command_prefix}invert <usuário>`\n🔀 **• Sinônimos:** `{command_prefix}inverter`",color = 0xff7b00)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/U62d9Oo.gif")
        await ctx.send(embed=embed)
    @help.command()
    async def mine(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        embed = discord.Embed(title = "Central de Ajuda - Mine",description = f"#️⃣ **• Nome:** `mine`\n🔍 **• Descrição:** `Edita o texto da imagem de conquista do Minecraft`\n⚙ **• Uso:** `{command_prefix}mine <texto>`",color = 0xff7b00)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/U62d9Oo.gif")
        await ctx.send(embed=embed)
    @help.command()
    async def laranjo(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        embed = discord.Embed(title = "Central de Ajuda - Laranjo",description = f"#️⃣ **• Nome:** `laranjo`\n🔍 **• Descrição:** `Edita o texto do meme do Laranjo`\n⚙ **• Uso:** `{command_prefix}laranjo <texto>`",color = 0xff7b00)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/U62d9Oo.gif")
        await ctx.send(embed=embed)
    @help.command(aliases = ["primeiraspalavras"])
    async def pp(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        embed = discord.Embed(title = "Central de Ajuda - Primeiras Palavras",description = f"#️⃣ **• Nome:** `pp`\n🔍 **• Descrição:** `Edita o texto do meme Primeiras Palavras`\n⚙ **• Uso:** `{command_prefix}pp <texto>`\n🔀 **• Sinônimos:** `{command_prefix}primeiraspalavras`",color = 0xff7b00)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/U62d9Oo.gif")
        await ctx.send(embed=embed)
    @help.command(aliases = ["sr_incrivel"])
    async def srincrivel(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        embed = discord.Embed(title = "Central de Ajuda - Sr. Incrível",description = f"#️⃣ **• Nome:** `srincrivel`\n🔍 **• Descrição:** `Cria um meme do Senhor Incrível`\n⚙ **• Uso:** `{command_prefix}srincrivel <num> <texto>`\n📚 **• Edições:** Para ver os números e todas as imagens disponíveis, digite apenas `{command_prefix}srincrivel`",color = 0xff7b00)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/U62d9Oo.gif")
        await ctx.send(embed=embed)
    @help.command()
    async def stonks(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        embed = discord.Embed(title = "Central de Ajuda - Stonks",description = f"#️⃣ **• Nome:** `stonks`\n🔍 **• Descrição:** `Edita o texto do meme Stonks`\n⚙ **• Uso:** `{command_prefix}stonks <texto>`",color = 0xff7b00)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/U62d9Oo.gif")
        await ctx.send(embed=embed)
    @help.command()
    async def notstonks(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        embed = discord.Embed(title = "Central de Ajuda - Not Stonks",description = f"#️⃣ **• Nome:** `notstonks`\n🔍 **• Descrição:** `Edita o texto do meme Not Stonks`\n⚙ **• Uso:** `{command_prefix}notstonks <texto>`",color = 0xff7b00)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/U62d9Oo.gif")
        await ctx.send(embed=embed)
    @help.command()
    async def tweet(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        embed = discord.Embed(title = "Central de Ajuda - Tweet",description = f"#️⃣ **• Nome:** `tweet`\n🔍 **• Descrição:** `Edita o texto de um tweet`\n⚙ **• Uso:** `{command_prefix}tweet <texto>`",color = 0xff7b00)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/U62d9Oo.gif")
        await ctx.send(embed=embed)
    @help.command()
    async def ytcomment(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        embed = discord.Embed(title = "Central de Ajuda - Yt Comment",description = f"#️⃣ **• Nome:** `ytcomment`\n🔍 **• Descrição:** `Edita o texto de um comentário do Youtube`\n⚙ **• Uso:** `{command_prefix}ytcomment <texto>`",color = 0xff7b00)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/U62d9Oo.gif")
        await ctx.send(embed=embed)
    @help.command(aliases = ["xcom"])
    async def xcomment(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        embed = discord.Embed(title = "Central de Ajuda - X Comment",description = f"#️⃣ **• Nome:** `xcomment`\n🔍 **• Descrição:** `Edita o texto de um comentário do \'X Vidros\'`\n⚙ **• Uso:** `{command_prefix}xcomment <texto>`\n🔀 **• Sinônimos:** `{command_prefix}xcom`",color = 0xff7b00)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/U62d9Oo.gif")
        await ctx.send(embed=embed)
#DIVERSOS - Diversos - diversos
    @help.command()
    async def botinfo(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        embed = discord.Embed(title = f"Central de Ajuda - Botinfo 『{Dpink}』",description = f"#️⃣ **• Nome:** `botinfo`\n🔍 **• Descrição:** `Mostra as informações sobre o bot e o seu desenvolvimento`\n⚙ **• Uso:** `{command_prefix}botinfo`",color = 0xe61ec8)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/zyyyizp.gif")
        await ctx.send(embed=embed)
    @help.command()
    async def dica(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        embed = discord.Embed(title = "Central de Ajuda - Dica",description = f"#️⃣ **• Nome:** `dica`\n🔍 **• Descrição:** `Mostra uma dica sobre o bot na DM`\n⚙ **• Uso:** `{command_prefix}dica`",color = 0xe61ec8)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/zyyyizp.gif")
        await ctx.send(embed=embed)
    @help.command(aliases = ["latence","latencia"])
    async def ping(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        embed = discord.Embed(title = f"Central de Ajuda - Ping 『{Dpink}』",description = f"#️⃣ **• Nome:** `ping`\n🔍 **• Descrição:** `Mostra a latência do bot`\n⚙ **• Uso:** `{command_prefix}ping`\n🔀 **• Sinônimos:** `{command_prefix}latence, {command_prefix}latencia`",color = 0xe61ec8)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/zyyyizp.gif")
        await ctx.send(embed=embed)
    @help.command()
    async def uptime(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        embed = discord.Embed(title = "Central de Ajuda - Uptime",description = f"#️⃣ **• Nome:** `uptime`\n🔍 **• Descrição:** `Mostra quando o bot foi ligado`\n⚙ **• Uso:** `{command_prefix}uptime`",color = 0xe61ec8)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/zyyyizp.gif")
        await ctx.send(embed=embed)
    @help.command(aliases = ["upvote"])
    async def vote(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        embed = discord.Embed(title = "Central de Ajuda - Vote",description = f"#️⃣ **• Nome:** `vote`\n🔍 **• Descrição:** `Manda o link para voto no Discord Bot List`\n⚙ **• Uso:** `{command_prefix}vote`\n🔀 **• Sinônimos:** `{command_prefix}upvote`",color = 0xe61ec8)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/zyyyizp.gif")
        await ctx.send(embed=embed)

    @help.command(aliases=["ac"])
    async def anicoin(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        embed = discord.Embed(title = "Central de Ajuda - Anicoin",description = f"#️⃣ **• Nome:** `anicoin`\n🔍 **• Descrição:** `Te da uma Anicoin (moeda do Anibot)`\n⚙ **• Uso:** `{command_prefix}anicoin`\n🔀 **• Sinônimos:** `{command_prefix}ac`",color = 0x9910ee)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/oQAvwWg.gif")
        await ctx.send(embed=embed)
    @help.command(aliases=["bal"])
    async def balance(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        embed = discord.Embed(title = "Central de Ajuda - Balance",description = f"#️⃣ **• Nome:** `balance`\n🔍 **• Descrição:** `Mostra o seu saldo de moedas atual`\n⚙ **• Uso:** `{command_prefix}balance`\n🔀 **• Sinônimos:** `{command_prefix}bal, {command_prefix}saldo`",color = 0x9910ee)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/oQAvwWg.gif")
        await ctx.send(embed=embed)
    @help.command()
    async def daily(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        embed = discord.Embed(title = "Central de Ajuda - Daily",description = f"#️⃣ **• Nome:** `daily`\n🔍 **• Descrição:** `Pega um bônus de moedas diário`\n⚙ **• Uso:** `{command_prefix}daily`",color = 0x9910ee)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/oQAvwWg.gif")
        await ctx.send(embed=embed)
    @help.command(aliases=["dep"])
    async def deposit(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        embed = discord.Embed(title = "Central de Ajuda - Deposit",description = f"#️⃣ **• Nome:** `deposit`\n🔍 **• Descrição:** `Deposita suas moedas no banco`\n⚙ **• Uso:** `{command_prefix}deposit <valor>`\n🔀 **• Sinônimos:** `{command_prefix}dep`",color = 0x9910ee)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/oQAvwWg.gif")
        await ctx.send(embed=embed)
    @help.command(aliases=["diceroll"])
    async def dice(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        embed = discord.Embed(title = "Central de Ajuda - Dice",description = f"#️⃣ **• Nome:** `dice`\n🔍 **• Descrição:** `Aposta suas moedas no jogo de dados`\n⚙ **• Uso:** `{command_prefix}dice <lado> <valor>`\n💬 **• Exemplo:** `{command_prefix}dice 4 10`\n🔀 **• Sinônimos:** `{command_prefix}diceroll`\n🎲 **• Lado correto:** `recebe o quádruplo do valor apostado`\n❌ **• Lado errado:** `perde o valor apostado`",color = 0x9910ee)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/oQAvwWg.gif")
        await ctx.send(embed=embed) 
    @help.command()
    async def flip(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        embed = discord.Embed(title = "Central de Ajuda - Flip",description = f"#️⃣ **• Nome:** `flip`\n🔍 **• Descrição:** `Aposta suas moedas no cara ou coroa`\n⚙ **• Uso:** `{command_prefix}flip <valor> <cara/coroa>`\n💬 **• Exemplo:** `{command_prefix}flip 10 cara`\n✅ **• Se vencer:** `recebe o dobro do valor apostado`\n❌ **• Se perder:** `perde o valor apostado`",color = 0x9910ee)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/oQAvwWg.gif")
        await ctx.send(embed=embed)
    @help.command()
    async def hourly(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        embed = discord.Embed(title = "Central de Ajuda - Hourly",description = f"#️⃣ **• Nome:** `hourly`\n🔍 **• Descrição:** `Pega um bônus de moedas por hora`\n⚙ **• Uso:** `{command_prefix}hourly`",color = 0x9910ee)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/oQAvwWg.gif")
        await ctx.send(embed=embed)
    @help.command(aliases = ["jkp","pedrapapeltesoura","ppt"])
    async def jankenpon(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        embed = discord.Embed(title = "Central de Ajuda - Jankenpon",description = f"#️⃣ **• Nome:** `jankenpon`\n🔍 **• Descrição:** `Jogue Pedra, Papel ou Tesoura`\n⚙ **• Uso:** `{command_prefix}jankenpon <comando>`\n<:ab_intAmarelo:911068181437771887> **• Comandos:** `pedra - papel - tesoura`\n🔀 **• Sinônimos:** `{command_prefix}jkp, {command_prefix}pedrapapeltesoura, {command_prefix}ppt`",color = 0x9910ee)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/oQAvwWg.gif")
        await ctx.send(embed=embed)
    @help.command(aliases=["lb"])
    async def leaderboard(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        embed = discord.Embed(title = "Central de Ajuda - Leaderboard",description = f"#️⃣ **• Nome:** `leaderboard`\n🔍 **• Descrição:** `Mostra os 3 usuários com mais moedas`\n⚙ **• Uso:** `{command_prefix}leaderboard`\n💡 **• OBS:** `O ranking é decidido baseado no valor total de moedas.`\n🔀 **• Sinônimos:** `{command_prefix}lb`",color = 0x9910ee)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/oQAvwWg.gif")
        await ctx.send(embed=embed)
    @help.command()
    async def pay(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        embed = discord.Embed(title = "Central de Ajuda - Pay",description = f"#️⃣ **• Nome:** `pay`\n🔍 **• Descrição:** `Doa suas moedas para um usuário`\n⚙ **• Uso:** `{command_prefix}pay <usuário> <valor>`",color = 0x9910ee)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/oQAvwWg.gif")
        await ctx.send(embed=embed)
    @help.command()
    async def rob(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        embed = discord.Embed(title = "Central de Ajuda - Rob",description = f"#️⃣ **• Nome:** `rob`\n🔍 **• Descrição:** `Rouba moedas de um usuário (de 1 à 1000 moedas)`\n⚙ **• Uso:** `{command_prefix}rob <usuário>`\n🔀 **• Sinônimos:** `{command_prefix}roubar`",color = 0x9910ee)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/oQAvwWg.gif")
        await ctx.send(embed=embed)
    @help.command()
    async def slot(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        embed = discord.Embed(title = "Central de Ajuda - Slot",description = f"#️⃣ **• Nome:** `slot`\n🔍 **• Descrição:** `Aposta suas moedas na roleta de corações`\n⚙ **• Uso:** `{command_prefix}slot <valor>`\n💬 **• Exemplo:** `{command_prefix}slot 10`\n💗 **• 3 corações iguais:** `recebe o triplo do valor apostado`\n💕 **• 2 corações iguais:** `recebe o valor apostado de volta`\n💔 **• 3 corações diferentes:** `perde o valor apostado`",color = 0x9910ee)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/oQAvwWg.gif")
        await ctx.send(embed=embed)
    @help.command(aliases=["wd"])
    async def withdraw(self, ctx):
        now = datetime.datetime.now()
        now = now.strftime("%d/%m/%Y - %H:%M:%S")
        embed = discord.Embed(title = "Central de Ajuda - Withdraw",description = f"#️⃣ **• Nome:** `withdraw`\n🔍 **• Descrição:** `Retira suas moedas do banco`\n⚙ **• Uso:** `{command_prefix}withdraw <valor>`\n🔀 **• Sinônimos:** `{command_prefix}wd`",color = 0x9910ee)
        embed.set_footer(text="Pedido por " + ctx.author.name + " em " + now, icon_url=ctx.author.avatar_url)
        embed.set_thumbnail(url="https://i.imgur.com/oQAvwWg.gif")
        await ctx.send(embed=embed)

def setup(bot):
    bot.add_cog(cog_div(bot))